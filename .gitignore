# Spec de travail détaillée, local uniquement, voir README.md pour le suivi public
SPEC.md
NOTES_*.md

# IDE
.vscode
.idea
*.code-workspace
.claude

# Dépendances (si une lib externe est ajoutée, ex. client Gmail API)
vendor/
composer.lock

# Cache PHPUnit
.phpunit.result.cache

# Zip généré par scripts/build-zip.sh
build/

# Secrets / identifiants (tokens OAuth Gmail, etc., ne doivent jamais être commités)
.env
*.env
credentials.json
token.json
*.pem
*.key

# Données réelles (.eml, XML de factures, PDF...) : jamais publiées, aucune exception.
# *.eml seul ne suffisait pas (raté une fois avec des .xml exportés séparément) : tout le
# dossier est exclu, purement local, voir tests/fixtures/README.md.
*.eml
tests/fixtures/real/

# OS
.DS_Store
